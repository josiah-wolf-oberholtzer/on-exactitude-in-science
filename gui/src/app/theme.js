import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#223344',
      light: '#223344',
      dark: '#223344',
      contrastText: '#fff',
    },
    secondary: {
      main: '#223344',
      light: '#223344',
      dark: '#223344',
      contrastText: '#fff',
    },
  },
});

export { theme };
